<h2>Faved authors
{{#if nonContacts}}
<a href="#" class="remove-contacts-filter">Remove filter</a>
{{else}}
<a href="#" class="non-contacts">Show only non-contacts</a>
{{/if}}
</h2>

<table class="table table-striped">
    <thead>
    <tr>
        <th>Author</th>
        <th>Count</th>
    </tr>
    </thead>
    <tbody>
        {{#each owners}}
        <tr>
            <td><a href="https://www.flickr.com/photos/{{owner}}/" target="_blank"><img src="http://flickr.com/buddyicons/{{owner}}.jpg"> <strong>{{owner_name}}</strong></a></td>
            <td>{{count}}</td>
        </tr>
        <tr>
            <td colspan="2">
            {{#each topphotos}}
                <a href="https://www.flickr.com/photos/{{photo.owner}}/{{photo.id}}" target="_blank"><img src="{{photo.urls.largeSquareThumb}}"></a>
            {{/each}}
            </td>
        </tr>
        {{/each}}
    </tbody>
</table>
<p><a href="#" class="moar">Show more ...</a></p>